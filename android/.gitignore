# Android / Gradle build outputs
/build
/app/build
.gradle
.kotlin

# NDK / native build
.cxx
.externalNativeBuild
# Cross-compiled C++ dependencies for the embedded server (produced locally / in CI)
/prebuilt

# IDE
.idea

# Local machine config and secrets
local.properties
*.jks
*.keystore
keystore.properties
