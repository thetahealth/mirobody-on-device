[package]
name = "mirobody-demo"
version = "0.1.0"
edition = "2021"
publish = false

# Dependency-free: the binding is a raw extern "C" block and the demo's HTTP
# probe uses std::net, so `cargo build` needs no network to fetch crates.

[[bin]]
name = "mirobody-demo"
path = "src/main.rs"
